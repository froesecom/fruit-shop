<a href="/">Place another order</a>
<div class="receipt">
  <% if @order.errors.any?  %>
    <h2>Sorry, that order cannot be fulfilled.</h2>
  <% else  %>
    <%= render partial: "order_items/order_item", collection: @order.order_items %> 
  <% end  %>
</div>
